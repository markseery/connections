# notification_skill configuration
subject_max_length: 500
default_throttle_per_min: 60
throttle_window_minutes: 1
smtp_timeout: 20
default_notification_limit: 100

route_risk:
  "POST /skills/notification_skill/send": safe
  "POST /skills/notification_skill/send/test": safe
  "GET /skills/notification_skill/notifications": safe
  "GET /skills/notification_skill/config": safe
  "GET /skills/notification_skill/stats": safe
