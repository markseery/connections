# Valid options for AI prompt parameters (e.g. site analysis, extraction).
# Use these as allowed values when passing category, type, etc. to the model.

dimensions:
  category:
    values:
      - location
      - energy
      - person
      - real_estate
      - offering
      - governance
      - compliance
      - community
      - other

  type:
    values:
      - hq
      - data_center
      - office
      - power
      - GPUs
      - land
      - product
      - solution
      - service
      - customer_support
      - policy
      - other

  fact:
    values:
      - any value - fact description
